# POIGAME LAB — AGENTS.md

このファイルは、POIGAME LABで作業するAIエージェントが必ず守る開発ルールである。
依頼内容とこのルールが衝突する場合は、安全性・データ保護・既存機能保護を優先する。

---

## 1. 基本原則

- 実装しただけで「完了」としない。
- 変更前に、対象ファイルと関連ファイルを確認する。
- 変更範囲は必要最小限にする。
- 依頼されていない大規模リファクタリングを行わない。
- 不明点を推測で本番仕様として実装しない。
- 既存の正常動作を壊さないことを最優先する。
- 問題を発見しても、依頼範囲外の修正は勝手に行わず報告する。
- 「より新しい」「より複雑」という理由だけで技術や構成を変更しない。

---

## 2. Git / 本番保護

- mainブランチへ直接pushしない。
- 本番コードを直接変更しない。
- コード変更は作業ブランチで行い、Pull Requestとして提出する。
- ユーザーの明示的な許可なくmergeしない。
- ユーザーの明示的な許可なくgit push、force push、履歴改変を行わない。
- 既存ファイルを削除する場合は、必要性を説明してから行う。
- GitHub Actions、Secrets、公開設定、ドメイン設定を勝手に変更しない。

---

## 3. 秘密情報・セキュリティ

- .env、APIキー、GitHub Secrets、トークン、パスワードなどの秘密情報を読み取ったり表示したりしない。
- 秘密情報をコード、ログ、Issue、Pull Request、コメント、HTML、CSV、JSONへ書き込まない。
- 秘密情報らしき値を発見した場合は内容を出力せず、存在だけを報告する。
- 外部サービスへの送信が必要な処理を勝手に追加しない。
- 不要な権限をGitHub Actionsへ追加しない。

---

## 4. データ保護

POIGAME LABではデータの正確性を最優先する。

- 取得失敗を「0件」や「案件なし」と解釈しない。
- 取得失敗時に、正常だった既存の検証済みデータを消さない。
- 根拠のない数値・報酬・達成日数・条件を生成しない。
- 推測値を検証済みデータとして公開しない。
- データ更新処理を変更する場合は、既存データ保持の安全機構を壊さない。
- CSV / JSONの列構造・キー構造を勝手に変更しない。
- データ形式を変更する場合は、読み込み側・書き込み側・テストへの影響をすべて確認する。

### 現在の主要データの役割

- games.csv:
  ゲーム自体の基本情報・表示情報を管理する。

- data/published_offers.csv:
  検証済み案件情報の公開用データとして扱う。

- offers.csv:
  既存のフォールバック用途があるため、役割を確認せず削除・統合しない。

これらは役割が異なるため、「重複している」という理由だけで一本化しない。

---

## 5. 攻略情報・体験情報

- 未確認情報を事実として公開しない。
- 実際にプレイしていない内容を「実体験」と表現しない。
- 攻略情報は、現在の検証パイプラインと根拠データを尊重する。
- 根拠URL・検証結果・採用条件を無視して記事へ直接書き込まない。
- 体験データと一般的な攻略情報を混同しない。
- 到達日数などの数値は、根拠が確認できるものだけを使用する。
- 「画像準備中」「内部メモ」「調査用コメント」など運営者向け情報を公開ページへ表示しない。

---

## 6. API・外部AI利用

- APIを無駄に呼び出さない。
- テストのためだけに本番APIを繰り返し実行しない。
- 同じ情報を取得するための重複API呼び出しを避ける。
- 可能な場合はモック、保存済みデータ、既存fixtureを優先する。
- 本番APIを使う必要がある場合は、呼び出し回数を最小化する。
- 外部AIの回答だけを根拠に検証済み情報へ昇格させない。

---

## 7. フロントエンド変更

HTML / CSS / JavaScriptを変更する場合は以下を確認する。

- PC表示だけでなくスマートフォン表示への影響を確認する。
- index.htmlだけでなく、関連するgame.htmlや攻略ページへの影響を確認する。
- 共通CSS / JavaScript変更時は複数ページを確認する。
- 不自然な改行、文字のはみ出し、横スクロール、ボタン崩れがないか確認する。
- 既存の検索、並び替え、カード表示、案件比較表示を壊していないか確認する。
- ユーザー向け画面に内部ID・デバッグ情報・内部ステータスを露出させない。
- デザイン変更は既存ブランド・レイアウトとの整合性を保つ。

---

## 8. Python / 自動化パイプライン変更

scripts/、config/、data/、GitHub Actionsを変更する場合は特に慎重に扱う。

- 変更前にデータフローを確認する。
- Phase 3 / Phase 4の既存処理を理解せずに構造変更しない。
- 正常系だけでなく、取得失敗・空レスポンス・不正データ・部分取得などの異常系を確認する。
- publication writeが不要なケースで公開データを書き換えない。
- atomic replaceなど既存のデータ保護機構を壊さない。
- 自動更新スケジュールや対象ゲームを勝手に変更しない。

---

## 9. 必須テスト

コード変更後は、可能な範囲で以下を実施する。

1. 構文・形式チェック
2. 対象機能の正常系テスト
3. 想定される異常系テスト
4. 関連機能の回帰テスト
5. データ破損・不要な書き込みがないことの確認
6. API呼び出し数・重複処理の確認
7. 変更差分の最終確認

既存のテストコマンドやREADMEの手順がある場合は、それを優先する。

テストを実行できなかった場合は、
「テスト済み」と書いてはいけない。
何を実行できなかったかを明示する。

テスト失敗が残っている状態で「完了」と報告しない。

---

## 10. 変更完了時の報告

作業終了時には必ず以下を報告する。

- 何を変更したか
- 変更したファイル
- なぜその変更が必要だったか
- 実施したテスト
- テスト結果
- 実施できなかった確認
- 既知のリスクや残課題
- APIを使用した場合は、その用途と可能なら呼び出し回数

「完了しました」だけの報告は禁止する。

---

## 11. 改善提案について

改善案を提案することは歓迎するが、提案と実装を区別する。

以下のような変更は、明示的な許可なく実行しない。

- フレームワーク導入
- SSR / SSGへの移行
- データベース導入
- CSV / JSON構造の大規模変更
- データ正典の変更
- 新しい外部サービス導入
- 新しい有料サービス導入
- 大規模CSSリファクタリング
- テストフレームワークの新規導入
- GitHub Actionsの大幅変更

必要だと思った場合は、メリット・デメリット・影響範囲を先に報告する。

---

## 12. 最優先ルール

POIGAME LABでは、

「速く変更すること」

よりも、

「正確なデータを守り、既存機能を壊さず、安全に変更すること」

を優先する。

迷った場合は、勝手に変更せず調査・報告を優先する。
