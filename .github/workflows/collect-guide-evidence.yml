name: Collect guide evidence (Phase 4 quarantine)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test dependency
        run: python -m pip install --disable-pip-version-check pytest
      - name: Collect directly verified guide pages (quarantine only)
        env:
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        run: python scripts/collect_guide_evidence.py
      - name: Extract evidence-bound guide claims (quarantine only)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scripts/extract_guide_claims.py
      - name: Evaluate guide claim support and conflicts (quarantine only)
        run: python scripts/evaluate_guide_claims.py
      - name: Verify Phase 4 quarantine contracts
        run: python -m pytest -q tests/test_phase4_guide_evidence_v49.py tests/test_phase4_guide_claims_v50.py tests/test_phase4_guide_claim_gate_v51.py
      - name: Upload quarantined evidence artifact
        uses: actions/upload-artifact@v4
        with:
          name: phase4-guide-evidence
          path: |
            data/guide_evidence.json
            data/guide_research_status.json
            data/guide_claims.json
            data/guide_claim_status.json
            data/guide_claim_decisions.json
            data/guide_claim_decision_status.json
          retention-days: 7
